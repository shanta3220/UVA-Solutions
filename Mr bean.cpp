#include <iostream>
using namespace std;

int main() {
	int t,l,w,h;
	cin >> t;
	while(t--){
	   cin >> l >> w >> h;
	   if( l <=20 && w <= 20 && h <= 20)
	    cout << "Case "<< I++ << ": good\n";
	    else cout << "Case " << I++ << ": bad\n";
	}
	return 0;
}
